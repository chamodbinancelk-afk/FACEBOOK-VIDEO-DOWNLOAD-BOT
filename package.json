{
  "name": "facebookdownbot",
  "version": "1.0.0",
  "description": "Telegram Bot for downloading Facebook videos, deployed on Cloudflare Workers.",
  "main": "src/local.js",
  "type": "module",
  "scripts": {
    "start": "bun run src/local.js",
    "build": "bun build ./src/worker.js --outdir ./dist --target=worker",
    "deploy": "npx wrangler deploy"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "grammy": "^1.38.3",
    "playwright": "^1.56.1"
  },
  "devDependencies": {
    "wrangler": "^3.114.15"
  }
}
